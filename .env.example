# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=appuser
DB_PASSWORD=apppassword
DB_NAME=helpdesk

# Authentication
AUTH_SECRET=your-secret-key-here-change-in-production

# Encryption Key for Base Knowledge (CRITICAL - Generate using: npx ts-node scripts/generate-encryption-key.ts)
# This key encrypts all documents and files in the knowledge base
# IMPORTANT: Keep this secret! Without it, encrypted data cannot be recovered.
ENCRYPTION_KEY=generate-a-64-character-hex-key-using-the-script-above
