## Banco de Dados (MariaDB - Docker Compose)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=helpdesk
DB_USER=appuser
DB_PASSWORD=apppassword
DB_ROOT_PASSWORD=rootpassword

# Prisma (somente dev)
# Usado por 'prisma migrate dev' para criar shadow DB (opcional)
SHADOW_DATABASE_URL="mysql://root:rootpassword@localhost:3306/helpdesk_shadow"

# Usuário padrão (seed)
DEFAULT_USER_EMAIL=admin@example.com
DEFAULT_USER_PASSWORD=admin123
DEFAULT_USER_NAME=Administrador

# JWT Secret (alterar em produção)
AUTH_SECRET=changeme-please